"/**\n * Cole este c├│digo no n├│ Code \"Preparar touchpoint\" do workflow SDR Outbound (n8n).\n * Avan├ºa touchpoints n├úo implementados nesta v1 (email, LinkedIn, liga├º├úo) sem travar a cad├¬ncia.\n */\n\nconst ORDER = [\n  { id: 'D1_WA', canal: 'whatsapp', dia: 1 },\n  { id: 'D3_WA', canal: 'whatsapp', dia: 3 },\n  { id: 'D5_EMAIL', canal: 'email', dia: 5 },\n  { id: 'D7_WA', canal: 'whatsapp', dia: 7 },\n  { id: 'D8_LI', canal: 'linkedin', dia: 8 },\n  { id: 'D10_WA', canal: 'whatsapp', dia: 10 },\n  { id: 'D13_LIG', canal: 'ligacao', dia: 13 },\n  { id: 'D14_WA', canal: 'whatsapp', dia: 14 },\n  { id: 'D17_EMAIL', canal: 'email', dia: 17 },\n  { id: 'D19_WA', canal: 'whatsapp', dia: 19 },\n  { id: 'D21_WA', canal: 'whatsapp', dia: 21 },\n];\n\nfunction nextSlotAfter(tpId, tier, email, linkedin) {\n  const idx = ORDER.findIndex((o) => o.id === tpId);\n  let i = idx + 1;\n  while (i < ORDER.length) {\n    const n = ORDER[i];\n    if (n.canal === 'email' && !email) { i++; continue; }\n    if (n.canal === 'linkedin' && !linkedin) { i++; continue; }\n    if (n.id === 'D13_LIG' && tier !== 'A') { i++; continue; }\n    return n;\n  }\n  return null;\n}\n\nfunction buildText(tpId, s) {\n  const nome = (s.metadata && s.metadata.nome_lead) || 'Oi';\n  const neg = (s.metadata && s.metadata.nome_negocio) || 'o restaurante';\n  const bairro = (s.metadata && s.metadata.bairro) || 'a regi├úo';\n  const map = {\n    D1_WA: `${nome}\\n\\nEstamos com um estudo de mercado em ${bairro} e o ${neg} chamou aten├º├úo. Posso te mandar o raio-x com score e ranking?`,\n    D3_WA: `${nome}, o material do ${neg} ficou pronto. S├│ preciso de um ok que te mando ­ƒæì`,\n    D7_WA: `${nome}, insistindo porque o estudo do ${neg} em ${bairro} ficou forte. Se tiver 2 min pra olhar, me d├í um toque.`,\n    D10_WA: `${nome}, fiz um resumo visual do estudo de voc├¬s. Quer ver o completo?`,\n    D14_WA: `${nome}, se eu pedisse o CMV exato do m├¬s passado, quanto tempo levaria pra responder? O estudo apontou oportunidades ÔÇö se quiser investigar junto, me fala.`,\n    D19_WA: `3 coisas do ${neg}: ranking no bairro, gap de CMV no segmento, oportunidade estimada. Quer ver o estudo completo?`,\n    D21_WA: `${nome}, como n├úo consegui retorno, encerro por aqui. O estudo do ${neg} segue dispon├¡vel; sucesso na opera├º├úo!`,\n  };\n  return map[tpId] || `Mensagem autom├ítica ÔÇö ${tpId}`;\n}\n\nfunction hoursFromNow(h) {\n  const t = new Date();\n  t.setHours(t.getHours() + h);\n  return t.toISOString();\n}\n\nfunction isUnsupportedInV1(def) {\n  return def.canal === 'email' || def.canal === 'linkedin' || def.canal === 'ligacao';\n}\n\nconst item = $input.first().json;\nlet tp = item.proximo_touchpoint_id || ORDER[0].id;\nconst tier = item.tier || 'B';\nconst email = item.email || (item.metadata && item.metadata.email) || '';\nconst linkedin = item.linkedin_url || (item.metadata && item.metadata.linkedin_url) || '';\nconst phone = item.phone_e164 || item.phone || '';\n\nlet guard = 0;\nwhile (guard++ < 24) {\n  const def = ORDER.find((o) => o.id === tp) || ORDER[0];\n  if (!isUnsupportedInV1(def)) break;\n  const n = nextSlotAfter(tp, tier, email, linkedin);\n  if (!n) break;\n  tp = n.id;\n}\n\nconst def = ORDER.find((o) => o.id === tp) || ORDER[0];\n\nlet skip = false;\nlet skipReason = '';\nif (def.canal === 'email' && !email) { skip = true; skipReason = 'sem_email'; }\nif (def.canal === 'linkedin' && !linkedin) { skip = true; skipReason = 'sem_linkedin'; }\nif (def.id === 'D13_LIG' && tier !== 'A') { skip = true; skipReason = 'tier_nao_A'; }\nif (def.canal === 'ligacao') { skip = true; skipReason = 'ligacao_manual'; }\n\nconst next = skip ? null : nextSlotAfter(tp, tier, email, linkedin);\nconst texto = skip ? '' : buildText(tp, item);\nconst proximoAt = skip ? hoursFromNow(24) : (next ? hoursFromNow(48) : null);\n\nlet statusFinal = 'em_cadencia';\nif (!skip && !next && tp === 'D21_WA') statusFinal = 'encerrado_breakup';\n\nconst touchDone = Array.isArray(item.touchpoints_executados) ? [...item.touchpoints_executados] : [];\nif (!skip && !touchDone.includes(tp)) touchDone.push(tp);\n\nreturn [{\n  json: {\n    session: item,\n    touchpoint_id: tp,\n    canal: def.canal,\n    skip,\n    skipReason,\n    texto_envio: texto,\n    phone_e164: phone,\n    proximo_touchpoint_id: next ? next.id : null,\n    proximo_envio_at: proximoAt,\n    status_final: statusFinal,\n    touchpoints_executados: touchDone,\n  },\n}];\n"
